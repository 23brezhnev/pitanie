-- Витрина строится от всех дат, а не только от дней с едой.
--
-- Раньше основой была таблица days, и пока еда была единственным источником,
-- это работало. После импорта из Health данных о здоровье стало в семь раз
-- больше: 3077 дней шагов против 404 дней с едой. Из них в витрине были видны
-- 119 — остальные взвешивания, шаги и замеры состава тела просто не доходили
-- до графиков.
--
-- has_food отличает день дневника от дня, где есть только здоровье: списку
-- дней нужны первые, графикам веса и шагов — все.
--
-- Полный текст представления применён к базе; ключевое изменение — источник
-- дат:
--
--   with vse_daty as (
--     select d from days
--     union select d from weight_log
--     union select d from steps_log
--     union select d from energy_log
--     union select d from body_log
--   )
--
-- и left join days поверх него вместо from days.
