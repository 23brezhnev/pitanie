-- Витрина берёт расход из Apple Health, где он есть, иначе считает формулой.
-- Полный текст представления — в базе; здесь только суть изменения.
--
-- expenditure_measured = энергия покоя + активная. Нужны обе половины: без
-- покоя получится расход меньше основного обмена, и баланс уедет в дефицит
-- на ровном месте.
--
-- expenditure = measured, если settings.prefer_measured_expenditure и
-- measured есть; иначе formula. expenditure_source говорит, что применилось,
-- чтобы в интерфейсе было видно, откуда число.
--
-- На norму и in_corridor расход по-прежнему не влияет.
